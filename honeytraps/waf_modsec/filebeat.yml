output:
  logstash:
    enabled: true
    hosts: '${LOGSTASH_HOST:?must set LOGSTASH_HOST env variable}'
    timeout: 15
   

filebeat:
  inputs:
    -
      paths:
        - /var/log/modsec_audit.log
      document_type: syslog
